name: flutter_application
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.0  # Actualiza a la versión más reciente
  http: ^1.2.2
  cupertino_icons: ^1.0.6
  permission_handler: ^10.0.0  # Asegúrate de tener esta dependencia para manejar permisos

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
